
apiVersion: apps/v1
kind: Deployment
metadata:
   name: nginx
   labels: 
     app: ubuntu-ds
   spec:
   replicas: 2
   selector: 
      matchLabels:
         app: ubuntu-ds
template:
 metadata:
   name: nginx
   labels:
     app: ubuntu-ds
   spec: 
   containers:
     - name: My-container
       image: shrikant712/docker:latest
       ports:
         containerPort: 80
